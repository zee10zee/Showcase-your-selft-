// var express = require("express");
// var router = express.Router();

// // routes

// user = require("../models/userDB")

// router.get("/follow/:user_id", (req, res)=>{
//     user.findById(req.params.follower_id, (err, foundUser)=>{
//         if(err){
//             console.log(err);
//         }else{
//             foundUser.followers.push(req.foundUser.user_id);
//             foundUser.save();
//             res.redirect("/user/" + req.params.user_id);

//         }

//     })
// })


// module.exports = router;